# Changelog

本文件记录会影响用户数据、导入结果、播放行为或发布包的主要变化。较小的界面调整可能只出现在提交记录中。

## [1.7.0-preview.1] - 2026-08-10

### NAS 根目录与断线恢复

- 状态格式升级到 v3，并从原有 `LibraryFolders` 自动迁移出带稳定 `RootId` 的根目录状态；旧路径列表继续同步保留，状态文件名和三代恢复布局不变。
- 本机盘、移动盘、光驱、UNC/SMB 与映射网络盘会记录在线、慢响应、离线、需要 Windows 凭据、最近在线时间、探测延迟和最近错误。
- 根目录与歌曲文件探测改为后台短超时任务；扫描入口、播放前检查、打开所在目录和视频旁挂字幕发现不再在 WPF 界面线程同步等待网络文件系统。
- NAS 失联时进入 `WaitingForRoot` 语义：停止重复重建 LibVLC，保留完整队列和时间点，按 1、2、5、10 秒退避，之后每 30 秒低频等待；目录恢复后才从原位置重建并继续。
- NAS 恢复但歌曲连续三次确认不存在时，会停止等待并明确提示重新扫描，不会把单个已移动文件误判成解码器崩溃。

### 播放会话、缓存与网络缓冲

- 持久化完整播放队列、当前索引、位置、循环模式、随机模式、最近随机历史、等待中的根目录和输出设备；即使关闭“启动自动播放”，下次启动仍会恢复原队列。
- 增加受容量上限约束的本机封面/歌词持久缓存。缓存使用来源长度、UTC 修改时间和签名记录，原子替换写入；NAS 离线时可读取最后一份本机资源，清理缓存不会修改媒体、NAS、歌单或曲库索引。
- UNC 与已识别映射网络盘使用可调的 5–30 秒 `file/disc/network-caching`；安全播放模式下 NAS 缓冲不少于 10 秒，本机播放仍保持原有低延迟参数。
- 设置页可查看每个曲库目录的类型、健康、延迟、最近在线和错误，可立即检测全部目录，并可调整探测超时、NAS 缓冲、等待重连、缓存开关与容量上限。

### 安全与资源边界

- 本机资源缓存不再使用会跟随目录联接的递归枚举；统计、清理和淘汰跳过 symlink/junction/reparse point，并在读、写、替换或删除前验证规范化目标仍位于缓存根目录。
- 缓存元数据上限 64 KiB、歌词缓存 16 MiB、封面 32 MiB、其他缓存 payload 64 MiB；缓存键长度和进程内条目锁数量也有固定上限。
- 歌词文件限制为 8 MiB，图片限制为 32 MiB 且解码边界为 1600×1600；背景图与播放历史使用同一套有界读取规则，避免损坏或恶意文件一次性耗尽内存。
- 状态文件限制为 256 MiB，单个状态字符串的 UTF-8 表示限制为 256 KiB；超限主状态不会进入模型或再复制一份诊断副本，而是继续尝试 `backup` 与 `previous`。写入超过上限也不会替换上一代有效主文件。
- 网易云歌单响应限制为 32 MiB、歌曲详情响应限制为 8 MiB；默认客户端禁止自动重定向和 Cookie，限制连接数、响应头、连接超时与歌单 ID 长度。
- 网易云模糊匹配增加标题首字符索引并把每首远端歌曲的候选图限制为最可信的 64 项；云 ID 清理改用索引，避免大型歌单与大型本地曲库形成无界平方级扫描。
- 曲库扫描跳过目录和媒体文件 reparse point，防止目录循环或越过所选根目录；跨平台预览源码同步了这些扫描、响应、历史、状态、封面和日志边界。
- 根目录和文件健康探测最多同时执行 4 项、最多保留 32 个不同在途探测；相同路径复用同一任务，队列满时快速返回，不再无界创建 `Task.Run`。

### 发布链

- GitHub Actions 默认只授予 `contents: read`，只有精确预览标签的 Release 作业获得 `contents: write`；检出不持久化凭据，官方 actions 固定到审计时的完整提交 SHA。
- Windows ZIP 改为排序、固定时间戳、流式写入的可复现打包脚本；脚本拒绝 publish 目录、文件或输出路径中的 symlink/junction，并继续生成独立 SHA-256 清单。
- 增加 Dependabot 的 GitHub Actions 与 NuGet 月度检查。NuGet 漏洞和弃用扫描均无命中；仅发现若干非安全性的可选小版本更新，本预览版未在缺少实机回归的情况下盲目升级播放/UI 依赖。

### 回归边界

- 扩展 `NasFoundationChecks`、`LibraryScanChecks`、网易云与推荐检查：使用系统临时目录、稀疏超大文件、模拟 HTTP 响应和临时 junction 验证上述边界。
- Windows Release、跨平台 Release 与全部 10 个确定性检查项目通过；大状态样本为 7,091,202 字节，干净克隆稳态保存约 62 ms、分配 89,224 字节。
- `AnalysisLevel=latest-all` 的 Windows/跨平台重建没有 `CA3xxx` 安全告警；NuGet 直接与传递依赖没有已知漏洞或弃用包。
- 没有启动主播放器，没有读取或改写真实曲库状态。真实 UNC/映射盘休眠、Wi-Fi 断线、Windows 凭据失效、DAC/HDMI 功放和实际续播仍需独立实机验收，因此以预览版而不是稳定版发布。

## [1.6.3] - 2026-08-08

### 无响应与状态保存热修复

- 修复 1.6.2 三代备份在每次保存时反复完整反序列化主文件、两级备份与复制文件的问题；有效状态现在按文件戳缓存，只在首次遇到或文件被外部改动后重新校验。
- 保留临时文件落盘、独占写锁、原子替换和 `main / backup / previous` 三代恢复语义；完整复制通过源文件有效性与字节长度检查后直接轮换，不再重复制造同一批对象。
- `TrackModel` 的时长文字、搜索文本、分类显示等 8 个纯界面计算字段不再写入 JSON；旧状态仍可直接读取，下一次保存会自动去除这些冗余字段。
- 退出流程改为异步等待最终保存，不再用同步等待阻塞 WPF 界面线程；保存较慢或前方已有保存任务时，窗口仍能处理消息。
- 启动时清理超过一小时的遗留状态临时文件，保留新鲜临时文件，避免误删仍可能属于活动写入的文件。
- 新增 6,000 首、至少 4 MiB 状态的性能回归：稳态保存分配不得超过状态大小两倍，耗时不得超过五秒，并继续验证三代轮换、失败传播和损坏恢复。

### 实测基准

- 在 5,917 首、原始约 8.19 MB 的只读状态副本上，1.6.2 修复前连续保存约 287–600 ms、每次分配约 36 MB。
- 1.6.3 连续保存约 31–48 ms、每次分配约 0.09–0.21 MB；去除冗余显示字段后，新状态约 5.84 MB。
- 基准只操作系统临时目录中的副本；没有启动播放器或改写真实曲库。

## [1.6.2] - 2026-08-08

### 网易云歌单导入

- 修复 100 首详情请求整批返回空时错误跳过小批次重试的问题；现在会拆成 25 首批次继续获取，覆盖 700 首以上歌单。
- 保留远端已经返回的完整歌曲 ID；歌曲详情临时缺失时不再把对应条目当作确定删除。
- 普通版与 `Off Vocal`、`Instrumental`、伴奏、纯音乐、无人声、无主唱、Backing Track 和日文オフボーカル等版本严格隔离。
- 使用全局一对一增广分配，避免前面的宽松匹配抢走后面歌曲的唯一候选。
- 将本地/远端时长纳入评分，并继续优先普通可播放文件而不是加密 NCM 候选。
- 自动移除历史错误云 ID 并写入正确文件；导入结果新增修正计数、详情暂缺数量和完整性状态。
- 支持 `feat.artist` 这类没有空格的紧凑标题后缀。
- Windows 完整版与跨平台预览源码增加对应的确定性回归测试。

### 扫描与大型曲库

- `TrackModel` 新增文件大小和 UTC 修改时间戳；未变化文件直接复用缓存元数据。
- 从旧状态升级时，可以先为既有歌曲补齐文件戳，不强制重新打开所有媒体标签。
- 新文件和发生变化的文件仍会重新读取标签、封面、歌词和时长。
- 可移动盘、网络盘和光驱限制为单路元数据读取，其他盘最多两路，降低随机读取抖动。
- 同一目录的封面旁路查找在一次扫描中复用结果。
- 文件枚举移到后台，并在目录与文件边界响应取消。
- 扫描取消后不替换内存曲库，也不写入状态文件；当前已阻塞的同步标签读取会在返回后停止。
- “重新识别社团”保留强制元数据刷新能力。

### 状态与崩溃防护

- 增加命名 Mutex 单实例保护，第二个实例在创建 LibVLC、热键和音频资源前退出。
- 保存状态前先序列化到同目录临时文件、刷新到磁盘并重新解析验证 JSON。
- 备份轮换改为 `main = 最新`、`backup = 上一代`、`previous = 上上代`。
- 主文件和第一备份同时损坏时，会继续尝试第二备份。
- 保存失败不再只写日志后吞掉异常；调用方能够得知保存没有成功。
- 跨保存方仍使用文件锁串行化写入，正常、失败和并发保存不会遗留本次临时文件。

### 文档与发布

- README 增加功能矩阵、网易云导入说明、状态文件布局、升级方式、隐私边界、构建验证和故障反馈信息。
- 新增网易云导入和数据安全专题文档。
- GitHub Actions 更新为 1.6.2，运行所有回归项目并生成 Windows x64 自包含压缩包与 SHA-256。

## [1.6.1] - 2026-08-06

- 增加标准、黑胶、沉浸歌词三种播放页面与可靠的返回曲库导航。
- 原文、音译、翻译、描边颜色和分轨透明度可以独立调整。
- 增加安全播放模式、播放停滞看门狗、有限恢复和引擎配置档案。
- 曲库根目录暂时离线时保留已有歌曲和歌单。
- 增加状态恢复、迷你播放器、任务栏控制、主题和更多确定性检查。

[1.6.3]: https://github.com/Shist1145/OfflineMusicLibrary/compare/v1.6.2...v1.6.3
[1.7.0-preview.1]: https://github.com/Shist1145/OfflineMusicLibrary/compare/v1.6.3...v1.7.0-preview.1
[1.6.2]: https://github.com/Shist1145/OfflineMusicLibrary/compare/v1.6.1...v1.6.2
[1.6.1]: https://github.com/Shist1145/OfflineMusicLibrary/releases/tag/v1.6.1
