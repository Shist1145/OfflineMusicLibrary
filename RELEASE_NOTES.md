# 本地音乐库 1.6.2

1.6.2 是一次面向“大歌单导入正确性”和“状态不因异常被覆盖”的稳定性更新。

## 网易云大歌单导入

- 修复歌曲详情 100 首整批返回空时没有继续小批重试的严重问题；现在会自动拆成 25 首批次，确定性测试覆盖 700 首歌单。
- 远端详情暂时不完整时保留歌曲 ID 和已有本地歌单条目，避免一次接口波动把已导入歌曲删掉。
- 普通版与 `Off Vocal`、`Instrumental`、伴奏、纯音乐、无人声、无主唱、Backing Track 和日文オフボーカル等版本严格隔离。
- 匹配改为全局一对一分配，避免前面的模糊歌曲抢走后面歌曲的唯一候选。
- 加入时长评分、可播放文件偏好和历史错误云 ID 修正。
- 支持 `feat.artist` 无空格标题，并在完成报告中区分完整 ID、已取详情、详情暂缺、精确匹配、模糊匹配和云 ID 修正数量。
- Windows 完整版和跨平台预览源码都加入相同的 700 首、Off Vocal 和全局分配回归检查。

## 大型曲库扫描

- 新增文件大小与 UTC 修改时间戳缓存；未变化文件不再重复打开 TagLib 读取标签和封面。
- 旧版索引首次升级时直接补齐文件戳，只读取新增或变化的文件。
- 导入前同步因此从“全量重读整个硬盘”改为快速增量同步。
- 可移动盘、网络盘和光驱使用单路读取，其他盘最多两路；同目录封面查找复用结果。
- 文件枚举在后台执行，扫描按钮可以取消；取消后不保存半成品曲库。
- “重新识别社团”仍可显式强制刷新所有可访问文件的元数据。

## 状态与反崩溃保护

- 增加单实例保护，第二个播放器实例会在创建 LibVLC、热键和音频资源前停止，避免两个实例最后互相覆盖歌单、播放次数与设置。
- 新状态先写入临时文件、刷新到磁盘并重新解析验证，再替换主文件。
- 状态轮换改为：`library-v2.json` 最新、`library-v2.backup.json` 上一代、`library-v2.previous.json` 上上代。
- 主文件损坏时依次尝试 backup 和 previous。
- 保存失败会真实返回异常，不再只记录日志后继续表现为成功。
- 跨保存方并发仍由独占文件锁串行化，并验证不遗留本次临时文件。

## 文档

- README 增加功能矩阵、安装/升级、网易云导入规则、状态目录、隐私、构建、测试、限制与问题反馈信息。
- 新增 `CHANGELOG.md`、网易云导入专题和数据安全/恢复专题。

## 验证

- 9 个确定性检查项目：专辑身份、增量曲库扫描、歌词、迷你播放器、Windows 网易云导入、跨平台网易云导入、歌单维护、推荐/历史、单实例与状态恢复。
- Windows Debug 与 Release 构建。
- Avalonia 跨平台 Release 构建。
- GitHub Actions 会在全新检出中还原依赖、运行全部检查，生成 Windows x64 自包含压缩包和 SHA-256。
- 本次维护没有自动启动或操控播放器界面；桌面渲染、真实音频设备和显卡路径仍属于人工验收范围。

## 兼容性

- Windows x64 完整版从 1.6.1 直接升级，不需要删除或迁移现有状态目录。
- 旧状态中的曲目没有文件戳时会向后兼容补齐。
- 已发布 Linux/macOS 包仍为 v1.4.0 预览版；本次跨平台改动目前体现在源码和测试中。
