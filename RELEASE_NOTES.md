# 本地音乐库 1.7.0-preview.1

这是 1.7.0 NAS Foundation 的第一份 Windows x64 预览版。它在 1.6.3 的状态保存、网易云大歌单和播放恢复基础上，补齐 NAS 断线语义、本机资源缓存，并修复安全审计发现的路径越界与资源耗尽风险。

本版本标记为预览版：自动回归已完成，但真实 NAS 休眠/唤醒、Wi-Fi 断线、Windows 凭据过期、USB DAC、HDMI 功放和多声道输出仍需要更多实机验收。需要最保守体验的用户可以继续使用 v1.6.3 稳定版。

## NAS 与播放会话

- 曲库根目录现在有稳定 ID、类型、在线/慢响应/离线/需要凭据状态、延迟、错误和最近在线时间。
- NAS 失联后保留曲库、完整队列、当前索引和播放位置，按 1、2、5、10、30 秒退避等待；恢复后才重建播放引擎。
- 保存队列顺序、循环/随机模式、最近随机历史、等待中的根目录和输出设备；关闭启动自动播放也不会丢掉队列。
- UNC 与映射网络盘可使用 5–30 秒缓冲；安全播放模式下 NAS 缓冲不少于 10 秒。
- 封面和歌词可缓存到本机，NAS 离线后仍可显示；容量和清理入口可在设置页控制。

## 安全与稳定性修复

- 修复 Dark 主题中的黑字、白底浅字和选中页签白底白字；普通文字、输入框、下拉框、列表、菜单与页签现在统一跟随主题，并有自动对比度回归。

- 缓存统计、清理和淘汰不再跟随 symlink/junction/reparse point；每次删除前都会确认目标仍在缓存根目录内。
- 状态、歌词、封面、播放历史、缓存 payload 与网易云响应都有大小上限，超限内容在完整读入或解码前拒绝。
- 主状态超过 256 MiB 时会继续尝试 `backup` 和 `previous`，不会复制另一份超大诊断文件，也不会覆盖有效恢复点。
- 封面和背景图限制为 32 MiB、最高 1600×1600 解码，避免压缩图片触发过量内存分配。
- 曲库扫描跳过目录和媒体文件的 reparse point，防止目录循环或扫描越过所选根目录。
- NAS/文件探测最多同时执行 4 项、最多保留 32 个不同在途任务；相同路径复用任务，队列满时快速返回。
- 网易云默认网络客户端禁止自动重定向和 Cookie，并限制连接、超时、响应头与响应体；大曲库匹配使用标题索引并限制候选图规模。
- 诊断日志采用有界队列、单条长度上限和轮换，避免错误风暴继续放大内存或磁盘压力。

## 网易云与跨平台源码

- 700 首详情批次重试、Off Vocal/伴奏隔离、全局一对一匹配、旧云 ID 修正和增量扫描全部保留。
- 标题候选索引降低大歌单对大型本地曲库的重复比较；每首远端歌曲只保留最可信的 64 个候选参与分配。
- 跨平台预览源码同步响应、历史、状态、封面、扫描和日志安全边界；本次 GitHub Release 仍只提供 Windows x64 完整版 ZIP。

## 发布与验证

- Windows Release 构建、跨平台 Release 构建和全部 10 个确定性检查项目通过。
- 6,000 首大状态样本为 7,091,202 字节；干净克隆本轮稳态保存约 62 ms、分配 89,224 字节。
- Windows 与跨平台在 `AnalysisLevel=latest-all` 下没有 `CA3xxx` 安全告警。
- Windows 与跨平台项目的直接/传递 NuGet 依赖均未发现已知漏洞或弃用包。
- 测试只使用临时目录、稀疏超大文件、模拟 HTTP 响应和临时 junction；没有启动或操控播放器，没有读取或改写真实曲库。
- GitHub Actions 使用最小权限和固定 action 提交；ZIP 由可复现打包脚本生成，并附 `SHA256SUMS-1.7.0-preview.1.txt`。
- 发布前本地自包含 ZIP 为 121,969,259 字节、900 个条目；完整性、路径穿越、无关架构、PDB 排除、重复打包一致性和 junction 拒绝均通过。官方 Release 资产以工作流生成的 SHA-256 清单为准。

## 更新方式

1. 彻底退出旧版。
2. 下载并完整解压 `OfflineMusicLibrary-1.7.0-preview.1-windows-x64.zip` 到新目录。
3. 可用同一 Release 中的 `SHA256SUMS-1.7.0-preview.1.txt` 核对 ZIP。
4. 直接运行 `OfflineMusicLibrary.exe`；无需复制或删除 `%LOCALAPPDATA%\OfflineMusicLibrary`。
5. 建议暂时保留旧程序目录；不要为排查界面问题删除 `library-v2.json`。

详细修复与仍未实机验证的边界见 README、CHANGELOG、`docs/DATA_SAFETY.md` 和 `docs/SECURITY_AUDIT_1.7.0-preview.1.md`。
