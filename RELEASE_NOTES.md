# 本地音乐库 1.6.3

1.6.3 是针对 1.6.2 状态保存性能回归和退出无响应的稳定性热修复。网易云大歌单、Off Vocal 隔离、增量扫描和三代恢复能力全部保留。

## 修复无响应

- 1.6.2 每次保存都会多次完整反序列化约 8 MB 的主状态和备份，导致一次保存分配约 36 MB；连续设置、切歌保存或退出等待时会形成明显停顿。
- 1.6.3 只在首次遇到状态文件或发现文件被外部改动时完整校验，之后复用已验证文件戳。
- 备份复制不再重复反序列化同一内容，仍检查来源有效性、复制长度、独占写锁和原子替换。
- 最终退出保存改为异步流程，不再同步堵住 WPF 界面线程。

## 更小的状态文件

- 时长文字、搜索文本、分类显示、播放次数显示等 8 个纯界面字段不再写入每一首歌的 JSON。
- 旧状态完全兼容；第一次由 1.6.3 保存后会自动去除冗余字段，不要求删除或重建曲库。
- 在 5,917 首的隔离副本上，新状态由约 8.53 MB 降至约 5.84 MB。

## 数据保护仍然保留

- `library-v2.json`：最新完整状态。
- `library-v2.backup.json`：上一代有效状态。
- `library-v2.previous.json`：上上代有效状态。
- 临时文件完成落盘后才替换主文件；失败会向调用方返回异常。
- 启动时只清理超过一小时的遗留状态临时文件，不会删除新鲜写入。

## 性能与回归验证

- 同一份约 8.19 MB 状态副本：修复前连续保存约 287–600 ms、分配约 36 MB；修复后约 31–48 ms、分配约 0.09–0.21 MB。
- 新增 6,000 首、至少 4 MiB 的大状态性能检查。
- 9 个确定性检查项目全部通过：专辑身份、增量扫描、歌词、迷你播放器、Windows/跨平台网易云导入、歌单维护、推荐/历史，以及单实例、状态轮换、损坏恢复、失败传播和大状态保存。
- Windows Release 构建通过；测试只使用临时目录，没有启动或操控播放器界面，也没有改写真实曲库。

## 兼容性

- Windows x64 完整版可从 1.6.1 或 1.6.2 直接升级。
- 不需要复制、删除或手动迁移 `%LOCALAPPDATA%\OfflineMusicLibrary`。
- 已发布 Linux/macOS 包仍为 v1.4.0 预览版；本次热修复针对 Windows 完整版状态存储与退出流程。
